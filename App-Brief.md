---
appName: "hw-write-to-raw"
externalId: "write-to-raw"
infra: "appsApi"
customer: "bgfast"
tier: "Tier 1: Monitoring & reporting"
owner: "Brent Groom <brent.groom@cognite.com>"
userCount: ""
businessValue: ""
milestones: ""
repoUrl: ""
userRole: "A Cognite Flows builder at a desk on a laptop, using this hello-world app to learn how to write rows into CDF RAW"
currentProblem: "A new Flows builder has no small working example of writing a row to CDF RAW, so they copy snippets from docs and still spend a session guessing the payload, auth, and table setup before anything lands in RAW."
oneSentenceStory: "As a Flows builder, I want to run a hello-world app that writes a fixed set of sample rows to CDF RAW so that I can copy a working pattern instead of assembling it from docs."
successCriteria: "A first-time Flows builder writes a visible row to a RAW table on the first try in under 10 minutes, instead of spending a full session piecing the call together."
userEvidence: "no user interviews yet; based on how new Flows builders typically learn CDF RAW from docs and samples"
reviewedSections:
  - appDetails
  - who
  - problem
  - tasksAndSuccess
---

# App Brief — hw-write-to-raw

## App details

- **Customer:** bgfast
- **Tier:** Tier 1: Monitoring & reporting
- **Owner:** Brent Groom <brent.groom@cognite.com>
- **Expected users:**
- **Business value:**
- **Milestones:**
- **Repository:**
- **App externalId:** write-to-raw
- **Infra:** appsApi

## Who is this app for?

A Cognite Flows builder at a desk on a laptop, using this hello-world app to learn how to write rows into CDF RAW

## What problem does this solve?

A new Flows builder has no small working example of writing a row to CDF RAW, so they copy snippets from docs and still spend a session guessing the payload, auth, and table setup before anything lands in RAW.

## Tasks and success

**One-sentence story.** As a Flows builder, I want to run a hello-world app that writes a fixed set of sample rows to CDF RAW so that I can copy a working pattern instead of assembling it from docs.

**Success criteria.** A first-time Flows builder writes a visible row to a RAW table on the first try in under 10 minutes, instead of spending a full session piecing the call together.

**User evidence.** no user interviews yet; based on how new Flows builders typically learn CDF RAW from docs and samples
