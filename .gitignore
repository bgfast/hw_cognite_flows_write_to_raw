# Dependencies
node_modules

# Build output
dist
coverage

# Retained app bundles from `cognite apps deploy` (signed before publish)
.cognite-bundles

# Local HTTPS certificates from `cognite apps setup-https`
certificates

# Environment variables
.env
.env.local

# Editor directories
.vscode
.idea
.cursor

# Local AI / Flows builder skills (pulled by `npx @cognite/cli apps skills pull`)
.agents
.claude
.mcp.json
skills-lock.json

# OS files
.DS_Store
Thumbs.db

